Game=True
cnt=0
#a function is a section the program that we call when we need it
def hint():
    global cnt #this will allow us to modify the variable all over the program
    if cnt==0:
    cnt+=1 #cnt=cnt +1
